import cv2 # OpenCV
import matplotlib as mtplb
import numpy as np
import argparse as ap
import pathlib

from tools.utils.process_text_file import ProcessTextFile


current_file_path = str(pathlib.Path(__file__).parent.absolute()) + "/data"

# text_file_handler = ProcessTextFile()
# self.camera_K_matrix_ = text_file_handler.read(camera_K_matrix_fname)
# self.camera_D_matrix_ = text_file_handler.read(camera_D_matrix_fname)

# meshgrid the corners of the square here


